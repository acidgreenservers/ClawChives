{
  "key_identifiers": [
    { "rule": "Session token prefix", "value": "api-", "verified": true },
    { "rule": "Session token length", "value": "32 bits (base62)", "verified": true },
    { "rule": "Session duration", "value": "24 hours", "verified": true },
    { "rule": "Auth header format", "value": "Bearer <token>", "verified": true },
    { "rule": "Modulo Bias Guard", "value": "Rejection sampling for 62 chars", "verified": true },
    { "rule": "One-Field Login", "value": "UNIQUE key_hash lookup", "verified": true }
  ],
  "encoding": "base62",
  "cryptographic_standards": {
    "hashing": "SHA-256",
    "encryption": "AES-256 (ShellCryption©™)",
    "comparison": "Constant-time (bitwise XOR)"
  },
  "api_endpoints": [
    {
      "endpoint": "POST /api/auth/token",
      "payload": {
        "keyHash": "string (SHA-256 hex)",
        "uuid": "string (UUID, optional for one-field)"
      },
      "verified": true
    },
    {
      "endpoint": "POST /api/agent-keys",
      "payload": {
        "name": "string",
        "permissions": {
          "canRead": "boolean",
          "canWrite": "boolean",
          "canEdit": "boolean",
          "canDelete": "boolean"
        },
        "expirationType": "string (never|date|duration)",
        "expirationDate": "string (ISO8601, optional)"
      },
      "verified": true
    }
  ],
  "access_control": {
    "middleware": ["requireAuth", "requirePermission", "requireHuman"],
    "permission_model": "Granular Lobster Permissions©™",
    "default_policy": "Deny all without valid Bearer token"
  },
  "compliance": {
    "standards": ["OWASP Top 10", "ClawKeys©™ Protocol"],
    "verified": true,
    "last_audit": "2026-03-14"
  }
}
